_metadata:
  # weblate-flags: max-length:32, ignore-same
  name: eval
  # weblate-flags: max-length:100
  description: eval code in slash context (bot developer only)

loading: "[ {icons.loading, emoji} Evaluating… ]"
