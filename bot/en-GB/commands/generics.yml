options:
  non-ephemeral:
    # weblate-flags: max-length:32
    name: public
    # Make sure this string does not exceed 100 characters when the {} is replaced. What doesn't fit will be discarded
    # weblate-flags: max-length:137
    description: "Whether you want the command to send messages visible for others in the channel (default is {default, select, true {True} false {False}})"
